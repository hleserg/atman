"""Living Codemap — auto-documentation generator for the Atman project.

Keeps SYSTEM_MAP.md, README.md, AGENTS.md, and .cursor/rules up to date
with the actual codebase using AST parsing (no LLM required for basic runs).
"""
