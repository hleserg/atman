# Endpoints Reference

> Auto-generated by `make codemap`. Do not edit.
> Source: `pyproject.toml` scripts, `docker-compose.yml`, AST CLI scan.

## CLI Commands (`[project.scripts]`)

| Command | Entry point |
|---------|-------------|
| `atman` | `atman.cli:main` |
| `atman-dev` | `atman.tui.app:main` |
| `atman-experience` | `atman.cli_experience:main` |
| `atman-identity` | `atman.cli_identity:main` |
| `atman-maintenance` | `atman.cli_maintenance:main` |
| `atman-skills` | `atman.skills.cli:main` |
| `atman-web` | `atman.web_dashboard:main` |
| `devui` | `atman.tui.app:main` |
| `webui` | `atman.web_dashboard:main` |

## CLI Commands (detected from source)

| Component | Command |
|-----------|---------|
| `eval` | `list_command` |
| `eval` | `run_command` |

## Infrastructure Ports (`docker-compose.yml`)

| Service | Ports | Purpose |
|---------|-------|---------|
| `postgres` | `127.0.0.1:5432:5432` | Relational store (facts, reflections, state) |
| `qdrant` | `127.0.0.1:6333:6333`, `127.0.0.1:6334:6334` | Vector search (experience retrieval) |

## HTTP API

FastAPI routes are defined in `src/atman/` (see `[api]` optional dependency).
Run `uvicorn atman.api:app --reload` and open `/docs` for the interactive Swagger UI.

